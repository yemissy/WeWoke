import React from 'react';
import { Button } from 'semantic-ui-react';

export default function LogoutButton(props){
  return(
    <div>
      {/* <Button color='black' size='small' id='logout' name='logout' onClick={props.onClick}>Log Out</Button> */}
    </div>
  )
}
